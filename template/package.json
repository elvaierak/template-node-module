{
  "scripts": {
    "build": "bili",
    "push": "git push",
<% if (testingFramework === TF_JEST) { %>
    "test": "jest",
<% } %>
    "publish": "echo \"not implemented\""
  },
  "devDependencies": {
<% if (testingFramework === TF_JEST) { %>
    "jest": "^22.4.3",
<% } %>
    "bili": "^1.6.0"
  },
  "bili": {
    "config": "./.bili.config.js"
  }
}
